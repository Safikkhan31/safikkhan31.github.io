# Copy to .env (or .env.local) — Vite only exposes vars prefixed with VITE_ to the browser.
# This is the React equivalent of the plain-HTML version's window.ASCENT_API_BASE_URL.

VITE_API_BASE_URL=http://localhost:5000/api
